<!DOCTYPE html>
<html lang="en">
	<head>
		<title>Birthday Tracker</title>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width,initial-scale=1.0" />
        <meta name="description" content="Birthday Tracker">
		<link
			rel="stylesheet"
			href="https://unpkg.com/modern-css-reset/dist/reset.min.css"
		/>
		<link rel="stylesheet" href="css/base.css" />
	</head>
	<body>
		<main>
			<h1>Welcome to Birthday Tracker</h1>
			<hr />
			<h2>Login</h2>
            <p class="error">
                <% if (error) { %>
                    <%= error %>
                <% } %> 
            </p>
			<form action="/login" method="POST">
                <div class="formGrid">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required placeholder="email@adress.com"/>
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required placeholder="Password"/>
                </div>
                <button type="submit">Login</button></td>
			</form>
			<button type="button" onclick="window.location.href='/register';">Register an Account</button>
		</main>
	</body>
</html>
