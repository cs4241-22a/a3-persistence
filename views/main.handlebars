<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Your Little Library</title>
    <meta charset="utf-8">
    <meta name="description" content="library">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
  </head>
  <body data-new-gr-c-s-check-loaded="14.1080.0" data-gr-ext-installed="">
    <h1 class="h1 text-center mt-5 fw-bold">Your Little Library</h1>
    <h2 class="h4 text-center mt-2 mb-5 fw-normal">Welcome to Your Little Library! Keep track of all your books with this helpful website!</h2>
    <div class="row mb-3 text-center">
      <div class="col-md-6 themed-grid-col">
        <h2 class="h2 mb-3 fw-normal">Your Bookshelf</h2>
          <table class="table table-primary">
            <thead>
            <tr>
              <th class="bookEntry">Book ID</th>
              <th class="bookEntry">Title</th>
              <th class="bookEntry">Author</th>
              <th class="bookEntry">Acquired Date</th>
              <th class="bookEntry">Read Yet?</th>
            </tr>
          </thead>
          <tbody>
            {{#each array}}
              <tr>
    	          {{#each this}}
                  <td>{{this}}</td>
                {{/each}}
              </tr>
            {{/each}}
          </tbody>
          </table>
      </div>
      <div class="col-md-6 themed-grid-col m-auto">
        <div class="mb-5 m-auto">
          <form action="/addBook" method="POST">
            <h3 class="h3 mb-3 fw-normal">Enter a new book here!</h3>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="addTitle" name="title" placeholder="Title" required/>
              <label for="addTitle">Title</label>
            </div>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="addAuthor" name="author" placeholder="Author" required/>
              <label for="addAuthor">Author</label>
            </div>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="addDate" name="acquiredDate" placeholder="Date" required/>
              <label for="addDate">Date Acquired (yyyy-mm-dd)</label>
            </div>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="addRead" name="readYet" placeholder="readYet" required/>
              <label for="addRead">Read Before? (Yes/No)</label>
            </div>
            <button class="w-100 btn btn-lg btn-primary" type="submit">Save to Library</button>
          </form>
        </div>
        <div class="w-50 mb-5 m-auto">
          <form action="/removeBook" method="POST">
            <h3 class="h3 mb-3 fw-normal">Enter the Book ID of the book you wish to burn:</h3>
            <div class="form-floating mb-2">
            <input class="form-control" type="text" id="bookID" name="bookID" required/>
            <label for="bookID">Book ID</label>
            </div>
            <p id="deleteButton"><button class="btn btn-lg btn-primary" type="submit">Burn Book</button></p>
          </form>
        </div>
        <div class="mb-5 m-auto">
          <form action="/editBook" method="POST">
            <h3 class="h3 mb-3 fw-normal">Enter the updated details of the book you wish to edit (please enter the same Book ID):</h3>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="editID" name="bookID" placeholder="BookID" required/>
              <label for="editID">Book ID</label>
            </div>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="editTitle" name="title" placeholder="Title" required/>
              <label for="editTitle">Title</label>
            </div>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="editAuthor" name="author" placeholder="Author" required/>
              <label for="editAuthor">Author</label>
            </div>
            <div class="form-floating mb-2"> 
              <input type="text" class="form-control" id="editDate" name="acquiredDate" placeholder="Date" required/>
              <label for="editDate">Date Acquired (yyyy-mm-dd)</label>
            </div>
            <div class="form-floating mb-2">
              <input type="text" class="form-control" id="editRead" name="readYet" placeholder="readYet" required/>
              <label for="editRead">Read Before? (Yes/No)</label>
            </div>
            <button class="w-100 btn btn-lg btn-primary" type="submit">Edit Book</button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
